@model IEnumerable<Lamarque_web.Models.servicios_bd>

@{
    ViewBag.Title = "Servicios";
}
<div class="img-responsive fondo-servicios">
    <h1 class="titulo">Servicios</h1>
</div>
<br />

@using (Html.BeginForm("Buscar", "servicios_bd", FormMethod.Post, htmlAttributes: new { @class = "navbar-form navbar-left" }))
{
    <form class="navbar-form navbar-left">

        <div class="input-group">

            <div class="form-group">
                <input name="busqueda_txt" type="text" class="form-control" placeholder="Buscar...">
            </div>
            <button id="busqueda" class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>

        </div>
    </form>
}

<table class="table table-striped">
    <tr class="info">
        <th>
            @Html.DisplayName("Tipo")
        </th>
        <th>
            @Html.DisplayName("Nombre")
        </th>
        <th>
            @Html.DisplayName("Contacto")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion)
            </td>
            <td>
                @*@Html.ActionLink("Editar", "Edit", new { id = item.Id })
    @Html.ActionLink("Ver +", "Details", new { id=item.Id }) |
    @Html.ActionLink("Eliminar", "Delete", new { id=item.Id })*@
            </td>
        </tr>
    }

</table>
@*Html.ActionLink("Agregar", "Create")*@
@Html.ActionLink("Volver arriba", "Index")
<br /><br />
@*Html.ActionLink("<-- Volver al Inicio", "Index", "Home")*@